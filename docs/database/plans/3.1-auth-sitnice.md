# Plan 3.1 — Auth sitnice

**Status:** 📝 plan potvrđen — spreman za implementaciju
**Datum plana:** 2026-07-17 · **Odluke potvrđene:** 2026-07-17 (sve preporuke prihvaćene)
**Referenca:** [model-review.md](../model-review.md) §3.1

## Izmene

### A. `auth.users` (izmena u mestu: `20260317_1010`)

1. **Email unique postaje partial** — briše se `uq_auth_users_email UNIQUE (email)`, dodaje se:
   ```sql
   CREATE UNIQUE INDEX uq_auth_users_email ON auth.users (LOWER(email)) WHERE NOT is_deleted;
   ```
   Soft-obrisan user oslobađa email (konzistentno sa roles); `LOWER()` štiti od duplikata po velikim/malim slovima (aplikacija i dalje normalizuje na lowercase pri upisu). Postojeći `ix_auth_users_email` se briše (partial unique ga pokriva).
2. **`email` VARCHAR(255) → VARCHAR(320)** — ujednačeno sa `external_logins`/`email_outbox` (RFC max je 320).
3. **Verifikacija emaila:** `email_verified_at TIMESTAMPTZ` (NULL = neverifikovan). Obavezno za samoregistraciju mušterija; postojeći nalozi koje kreira radnik dobijaju verifikaciju kroz prvi login link (aplikativno).
4. **Lockout:** `failed_login_count SMALLINT NOT NULL DEFAULT 0` + `locked_until TIMESTAMPTZ`. Aplikacija: increment na neuspeh, reset na uspeh, zaključavanje posle N pokušaja (N i trajanje su config, ne DB).
5. **MFA checkovi:** `ck_auth_users_mfa_method CHECK (mfa_method IS NULL OR mfa_method IN ('totp','sms','email'))` + `ck_auth_users_mfa_enabled CHECK (NOT mfa_enabled OR mfa_method IS NOT NULL)`.

### B. `auth.login_history` (izmena u mestu: `20260317_1200`)

`user_id` postaje nullable + nova kolona `attempted_email VARCHAR(320)` — pokušaj prijave sa nepostojećim emailom se sada beleži (bitno za security monitoring). `CHECK (user_id IS NOT NULL OR attempted_email IS NOT NULL)`.

### C. Nova tabela `auth.email_verification_tokens`

Nova skripta: `20260522_1120_T_email_verification_tokens_DDL.sql` — identičan obrazac kao `password_reset_tokens` (user_id, token_hash SHA-256 unique, expires_at, used_at; append-only, bez audit kolona).

### D. Email template za verifikaciju

Nova skripta: `20260522_1130_T_email_templates_email_verification_DML.sql` — seed template `email_verification` (en/sr), po obrascu postojećeg password_reset seed-a.

## Test plan

1. Rebuild prolazi.
2. Dva aktivna usera sa `Pera@x.com` / `pera@x.com` → partial unique odbija; soft-delete prvog → drugi se registruje.
3. login_history red bez user_id ali sa attempted_email → prolazi; bez oba → CHECK odbija.
4. `mfa_enabled=TRUE` bez metode → CHECK odbija; `mfa_method='foo'` → CHECK odbija.

## Odluke — ✅ potvrđene 2026-07-17

1. **Partial unique na `LOWER(email)`** (case-insensitive + oslobađanje po soft delete) — ✅ da.
2. **Lockout pragovi u konfiguraciji aplikacije**, ne u bazi — ✅ da.
3. **Poseban token table + template za verifikaciju** (obrazac password_reset) — ✅ da.
